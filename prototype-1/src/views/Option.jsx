

function Option() {
    return (   
        <div class="flex justify-center items-center">
            <a href={`/SetUp/Welcome`}>Viewer</a>
            <a href={`/StreamerX`}>Streamer</a>
        </div>
    );
}

export default Option